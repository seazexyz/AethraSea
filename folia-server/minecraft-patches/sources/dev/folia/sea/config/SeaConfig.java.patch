--- /dev/null
+++ b/dev/folia/sea/config/SeaConfig.java
@@ -1,0 +_,177 @@
+package dev.folia.sea.config;
+
+import com.mojang.logging.LogUtils;
+import org.bukkit.configuration.file.YamlConfiguration;
+import org.slf4j.Logger;
+
+import java.io.File;
+import java.io.IOException;
+import java.nio.charset.StandardCharsets;
+import java.nio.file.Files;
+
+public final class SeaConfig {
+
+    private static final Logger LOGGER = LogUtils.getLogger();
+    private static final File CONFIG_FILE = new File("sea.yml");
+    private static YamlConfiguration config;
+
+    // Brand settings
+    public static String f3BrandName = "AethraSea";
+    public static String serverName = "AethraSea";
+
+    // Performance settings
+    public static boolean smartRegionMerging = true;
+    public static int regionMergeHysteresisTicks = 20;
+    public static boolean asyncChunkIo = true;
+    public static boolean asyncPathfinding = true;
+    public static int pathfindingThreads = 4;
+
+    // Gameplay settings
+    public static boolean stasisChamberFix = true;
+    public static boolean smoothCrossRegionFluids = true;
+    public static boolean crossRegionVehicleRubberbandFix = true;
+    public static boolean crossRegionVillagerPoiSync = true;
+
+    // Network settings
+    public static boolean packetFloodLimiter = true;
+    public static int maxPacketsPerSecond = 500;
+    public static boolean regionPacketRateLimit = true;
+    public static boolean asyncPacketEvents = true;
+
+    // Security & Stability settings
+    public static boolean gracefulRegionRecovery = true;
+    public static boolean bookSignPayloadValidator = true;
+    public static int maxSignTextLength = 384;
+    public static int maxBookPages = 100;
+    public static int maxBookPageLength = 1024;
+
+    // GUI settings
+    public static boolean guiEnabled = true;
+
+    // Performance monitor settings (drives /tpsbar, /rambar and automatic warnings)
+    public static boolean monitorWarnEnabled = true;
+    public static double monitorWarnTpsThreshold = 18.0;
+    public static int monitorWarnRamPercent = 85;
+
+    private SeaConfig() {}
+
+    public static void init() {
+        if (!CONFIG_FILE.exists()) {
+            createDefaultConfig();
+        }
+        loadConfig();
+    }
+
+    private static void createDefaultConfig() {
+        final String defaultConfig = """
+            # AethraSea Hybrid Engine configuration.
+            # Configure the engine's threading, networking, security and monitoring behaviour.
+            #
+            # Wired behaviour (these values actually change how the engine runs):
+            #   performance.async-chunk-io           - load/generate chunks on worker threads
+            #   performance.async-pathfinding        - run mob path-finding on dedicated threads
+            #   performance.pathfinding-threads      - number of path-finding worker threads
+            #   network.packet-flood-limiter         - drop + kick clients exceeding max-packets-per-second
+            #   network.max-packets-per-second       - packet flood threshold per connection
+            #   network.async-packet-events          - route packets through the async event pipeline
+            #   performance.monitor-warning.*        - auto action-bar warnings to ops on low TPS / high RAM
+            #   gui.enabled                          - enables /seagui and the in-game status GUI
+
+            brand:
+              f3-brand-name: 'AethraSea'
+              server-name: 'AethraSea'
+
+            performance:
+              smart-region-merging: true
+              region-merge-hysteresis-ticks: 20
+              async-chunk-io: true
+              async-pathfinding: true
+              pathfinding-threads: 4
+              monitor-warning:
+                enabled: true
+                tps-threshold: 18.0
+                ram-percent-threshold: 85
+
+            gameplay:
+              stasis-chamber-fix: true
+              smooth-cross-region-fluids: true
+              cross-region-vehicle-rubberband-fix: true
+              cross-region-villager-poi-sync: true
+
+            network:
+              packet-flood-limiter: true
+              max-packets-per-second: 500
+              region-packet-rate-limit: true
+              async-packet-events: true
+
+            security:
+              graceful-region-recovery: true
+              book-sign-payload-validator: true
+              max-sign-text-length: 384
+              max-book-pages: 100
+              max-book-page-length: 1024
+
+            gui:
+              enabled: true
+            """;
+        try {
+            Files.write(CONFIG_FILE.toPath(), defaultConfig.getBytes(StandardCharsets.UTF_8));
+            LOGGER.info("[SeaConfig] Created default sea.yml configuration.");
+        } catch (IOException e) {
+            LOGGER.error("[SeaConfig] Could not save default sea.yml", e);
+        }
+    }
+
+    public static void loadConfig() {
+        try {
+            config = YamlConfiguration.loadConfiguration(CONFIG_FILE);
+
+            // Brand
+            f3BrandName = config.getString("brand.f3-brand-name", "AethraSea");
+            serverName = config.getString("brand.server-name", "AethraSea");
+
+            // Performance
+            smartRegionMerging = config.getBoolean("performance.smart-region-merging", true);
+            regionMergeHysteresisTicks = config.getInt("performance.region-merge-hysteresis-ticks", 20);
+            asyncChunkIo = config.getBoolean("performance.async-chunk-io", true);
+            asyncPathfinding = config.getBoolean("performance.async-pathfinding", true);
+            pathfindingThreads = config.getInt("performance.pathfinding-threads", Math.max(2, Runtime.getRuntime().availableProcessors() / 2));
+
+            // Performance monitor
+            monitorWarnEnabled = config.getBoolean("performance.monitor-warning.enabled", true);
+            monitorWarnTpsThreshold = config.getDouble("performance.monitor-warning.tps-threshold", 18.0);
+            monitorWarnRamPercent = config.getInt("performance.monitor-warning.ram-percent-threshold", 85);
+
+            // Gameplay
+            stasisChamberFix = config.getBoolean("gameplay.stasis-chamber-fix", true);
+            smoothCrossRegionFluids = config.getBoolean("gameplay.smooth-cross-region-fluids", true);
+            crossRegionVehicleRubberbandFix = config.getBoolean("gameplay.cross-region-vehicle-rubberband-fix", true);
+            crossRegionVillagerPoiSync = config.getBoolean("gameplay.cross-region-villager-poi-sync", true);
+
+            // Network
+            packetFloodLimiter = config.getBoolean("network.packet-flood-limiter", true);
+            maxPacketsPerSecond = config.getInt("network.max-packets-per-second", 500);
+            regionPacketRateLimit = config.getBoolean("network.region-packet-rate-limit", true);
+            asyncPacketEvents = config.getBoolean("network.async-packet-events", true);
+
+            // Security
+            gracefulRegionRecovery = config.getBoolean("security.graceful-region-recovery", true);
+            bookSignPayloadValidator = config.getBoolean("security.book-sign-payload-validator", true);
+            maxSignTextLength = config.getInt("security.max-sign-text-length", 384);
+            maxBookPages = config.getInt("security.max-book-pages", 100);
+            maxBookPageLength = config.getInt("security.max-book-page-length", 1024);
+
+            // GUI
+            guiEnabled = config.getBoolean("gui.enabled", true);
+
+            LOGGER.info("[SeaConfig] Loaded sea.yml | Brand: {}, Async I/O: {}, Async Pathfinding: {}, Packet Limiter: {}, Monitor Warnings: {}",
+                    f3BrandName, asyncChunkIo, asyncPathfinding, packetFloodLimiter, monitorWarnEnabled);
+        } catch (Exception e) {
+            LOGGER.error("[SeaConfig] Failed to load sea.yml, using default values", e);
+        }
+    }
+
+    public static boolean isMonitorWarningEnabled() {
+        return monitorWarnEnabled;
+    }
+}
